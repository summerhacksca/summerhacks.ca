/**
 * Toggle this flag to `true` when applications are closed.
 *
 * When enabled:
 *  - The hero section main text changes to "Applications are now closed."
 *  - All non‑homepage routes are redirected back to `/`.
 */
export const APPLICATIONS_CLOSED = true;
